version: "2"
services:

  auth:
    image: openlmis/auth
    links:
      - db
      - log
    env_file: .env

  db:
    image: openlmis/postgres:9.6-debezium
    env_file: .env

  log:
    image: openlmis/rsyslog:1
    volumes:
      - '../logs/auth:/var/log'
