version: "2"
services:

  auth:
    image: openlmis/auth
    ports:
      - "8080"
    links:
      - db
      - log
    env_file: .env

  db:
    image: openlmis/postgres
    env_file: .env

  log:
    image: openlmis/rsyslog
    volumes:
      - '../logs/auth:/var/log'
